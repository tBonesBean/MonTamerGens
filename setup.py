"""
A minimal setup.py to support legacy build systems.

Most of the project metadata is defined in pyproject.toml. This file just
enables setuptools to build the project.
"""
from setuptools import setup

if __name__ == "__main__":
    setup()
